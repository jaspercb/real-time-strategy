#pragma once

#include "SDL2/SDL.h"

namespace TeamColor {
	enum Enum {
		Null, // for when we don't want to recolor
		Pink,
		Red,
		Blue,
		Green,
		Purple,
		Orange,
		Brown,
		Yellow,
		Teal,
		Pale,
		Iris,
		Olive,
		num
	};
};
