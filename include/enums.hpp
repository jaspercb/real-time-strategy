#pragma once

#include "SDL2/SDL.h"

enum TeamColor{
	COLOR_NULL, // for when we don't want to recolor
	COLOR_PINK,
	COLOR_RED,
	COLOR_BLUE,
	COLOR_GREEN,
	COLOR_PURPLE,
	COLOR_ORANGE,
	COLOR_BROWN,
	COLOR_YELLOW,
	COLOR_TEAL,
	COLOR_PALE,
	COLOR_IRIS,
	COLOR_OLIVE,
	NUM_TEAMCOLORS
};
